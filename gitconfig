[user]
	name = yoshihiko.shiraki
	email = shirakiya.pv@gmail.com
[filter "media"]
	clean = git-media-clean %f
	smudge = git-media-smudge %f
[core]
	editor = vim
	excludesfile = ~/.gitignore
[color]
    status = auto
    diff = auto
    branch = auto
    interactive = auto
    grep = auto
[alias]
	current-branch = rev-parse --abbrev-ref HEAD
	push = !push origin `git current-branch`
[ghq]
	root = ~/.ghq
	root = ~/.go/src
